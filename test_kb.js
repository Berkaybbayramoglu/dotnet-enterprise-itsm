const http = require('node:http');

const data = JSON.stringify({
    username: 'testuser',
    password: 'Testuser1!' // assuming this user exists or I can use another
});

// Since I don't know the exact username/password of user 11, let's create a JWT token manually or just look at the code.
